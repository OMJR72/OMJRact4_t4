info:
  name: ObtenerEstudiantes
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8088/api/estudiantes/obtener
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
